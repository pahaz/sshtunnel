[tox]
envlist = py34,py27

[testenv]
commands =
    python -V
    pip install -r test_requirements.txt
    python setup.py install
    py.test --showlocals --debug --cov-config .coveragerc --cov sshtunnel --durations=10 tests.py
